PORT=5000
MONGO_URI=mongodb://localhost:27017/visionhire
JWT_SECRET=your_super_secret_jwt_key_change_this

# ── AI PROVIDERS (add at least one) ──────────────────────────────
# OPTION 1: Groq — FREE, no billing needed, 14,400 req/day
# Get key in 30 seconds: https://console.groq.com → API Keys → Create
GROQ_API_KEY=your_groq_api_key_here

# OPTION 2: Google Gemini — free tier (limited quota)
# Get key: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

NODE_ENV=development
